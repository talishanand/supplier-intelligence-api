fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sqlalchemy>=2.0.36
asyncpg>=0.30.0
aiosqlite>=0.20.0
greenlet>=3.1.0
httpx>=0.27.2
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.1
anthropic>=0.69.0

# Optional. Entity resolution falls back to a pure-python char-ngram vectorizer
# when neither of these is available, so the demo runs with zero paid APIs.
# openai>=1.54.0
# sentence-transformers>=3.3.0
